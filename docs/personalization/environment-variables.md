# Environment Variables

If a skill requires environment variables, list them in frontmatter `env:` and add a `.env.example` to the skill folder.

Example:

```bash
# skills/example-skill/.env.example
API_KEY=your_value_here
```
