# Skill Structure

A skill is a folder with a `SKILL.md` at the root.

Example:

```
skills/example-skill/
  SKILL.md
  references/
  scripts/
  user/
```

Supporting files are optional. Reference them directly from the skill content.
